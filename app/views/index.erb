<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title>Recipes</title>
  </head>
  <body>

    <h1>Welcome!</h1>

    <% @recipes.each.with_index do |recipe, i| %>
      <p>
        <a href="/recipes/<%= recipe.id %>"> <%= i + 1 %>: <%= recipe.name %> </a>
      </p>
    <% end %>

  </body>
</html>
